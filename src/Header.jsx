import React from 'react';
import "./Header.css";

const AnimeCatalog = () => {
    return (
        <header>
            <h2>aniru</h2>
            <input type="text"/>
            <button>Войти</button>
        </header>
    );
};

export default AnimeCatalog;